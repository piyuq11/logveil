# Examples

The sample log contains synthetic values only. Try:

```bash
logveil examples/sample.log --report
```

Use it to verify a local installation without copying real credentials into a test case.

